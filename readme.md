# CSV fit to Azure Table Stroage

Replace `account_name` and `account_key` with your account's credentials

> This script can be improve and  we can reduce dependencies but I just wanted to get job done.